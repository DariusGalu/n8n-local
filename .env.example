# Public domain where n8n will be available
DOMAIN=n8n.darius-galu.com

# Extra HTTP Basic Auth in front of n8n (recommended)
N8N_BASIC_USER=admin
N8N_BASIC_PASS=changeme

# Generate once: `openssl rand -hex 32`
# Keep it forever; changing it later breaks decryption of saved creds.
N8N_ENCRYPTION_KEY=GENERATE_A_64_HEX_KEY

# Your timezone
TZ=Europe/Bucharest